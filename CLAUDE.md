# JEDCO Dashboard - قواعد المشروع

> ⚠️ **هذا الملف يحتوي على قواعد حرجة يجب اتباعها بدقة**

---

## 🚨 قواعد حماية البيانات الحرجة

### ❌ ممنوع منعاً باتاً:

```
1. لا تعدل الملف الأصلي JEDCO_HT.xlsx أبداً
2. لا تحذف أي بيانات من أي مصدر
3. لا تكتب في مجلد input/
4. لا تنقل أو تنسخ الملف الأصلي
5. لا تستخدم pandas.to_excel() على الملف الأصلي
6. لا تفتح الملف بوضع write أو append
```

### ✅ مسموح:

```
1. القراءة فقط من الملف الأصلي (read_only=True)
2. الكتابة في مجلد output/ فقط
3. إنشاء نسخ احتياطية في backups/
4. تصدير JSON/CSV في output/
```

---

## 📁 المسارات

### الملف الأصلي (READ-ONLY):
```
المسار: C:\Users\pcpz1\Desktop\JEDCO_Fail_HT\JEDCO_Tabel_Record\JEDCO_HT.xlsx
الورقة: التسجيل
صف العناوين: 5
```

### مجلدات المشروع:
| المجلد | الغرض | الصلاحية |
|--------|-------|----------|
| `input/` | بيانات مصدرية منسوخة | READ-ONLY |
| `output/` | نتائج المعالجة | READ-WRITE |
| `scripts/` | سكربتات Python | READ-WRITE |
| `backups/` | نسخ احتياطية | WRITE-ONLY |

### مجلد الأرشيف:
```
المسار: C:\Users\pcpz1\Desktop\JEDCO_Fail_HT\JEDCO_Data_Monthly\
نمط التسمية: JEDCO_YYYY-MM-DD.xlsx
```

---

## 📊 الأعمدة المطلوبة (16 عمود أزرق)

| # | اسم العمود | الموقع | النوع | الوصف |
|---|-----------|--------|-------|-------|
| 1 | م | A | int | معرف فريد (Primary Key) |
| 2 | رقم اللوحة | B | str | رقم لوحة الحافلة |
| 3 | الناقل البري | C | str | اسم شركة النقل |
| 4 | رقم الرحلة | D | str | رقم الرحلة |
| 5 | عدد الرحلات | E | int | عدد الرحلات |
| 6 | عدد الركاب | F | int | عدد الركاب |
| 7 | نوع التأشيرة | G | str | نوع التأشيرة |
| 8 | وقت الإقلاع | H | time | وقت إقلاع الحافلة |
| 9 | وقت وصول الحافلة | I | time | وقت الوصول |
| 10 | شركة العمرة | J | str | اسم شركة العمرة |
| 11 | حالة الرحلة | K | str | حالة الرحلة |
| 12 | وضع الرحلة | L | str | وضع الرحلة |
| 13 | حالة الحجز | M | str | حالة الحجز |
| 14 | الجنسية | N | str | جنسية الركاب |
| 15 | الوجهة | O | str | وجهة الرحلة |
| 16 | الإجراء المتخذ | P | str | الإجراء |

### الأعمدة المُتجاهلة (Q-U):
- حالة التفويج، نوع التفويج، شركة الخدمات، يحتاج محضر، رمز النوبة

---

## 🐍 تعليمات السكربتات

### قالب قراءة البيانات الآمن:

```python
import pandas as pd
from pathlib import Path

# المسارات
EXCEL_PATH = Path(r"C:\Users\pcpz1\Desktop\JEDCO_Fail_HT\JEDCO_Tabel_Record\JEDCO_HT.xlsx")
OUTPUT_DIR = Path(__file__).parent.parent / "output"

# الأعمدة المطلوبة (A-P = 0-15)
REQUIRED_COLUMNS = list(range(16))

# قراءة آمنة فقط
def read_data():
    """قراءة البيانات بشكل آمن - READ ONLY"""
    df = pd.read_excel(
        EXCEL_PATH,
        sheet_name="التسجيل",
        header=4,  # صف العناوين = 5 (index 4)
        usecols=REQUIRED_COLUMNS,
        engine='openpyxl'
    )
    return df

# الكتابة في output فقط
def save_output(df, filename):
    """حفظ النتائج في مجلد output فقط"""
    output_path = OUTPUT_DIR / filename
    # تأكد أن المسار في output
    assert "output" in str(output_path), "يجب الحفظ في مجلد output فقط!"
    df.to_json(output_path, orient='records', force_ascii=False)
```

### قواعد كتابة السكربتات:

1. **استخدم read_only دائماً:**
   ```python
   pd.read_excel(..., engine='openpyxl')  # openpyxl يقرأ فقط افتراضياً
   ```

2. **لا تستخدم أبداً:**
   ```python
   # ❌ ممنوع
   df.to_excel(EXCEL_PATH)  # الكتابة على الملف الأصلي
   open(EXCEL_PATH, 'w')    # فتح للكتابة
   shutil.move(EXCEL_PATH)  # نقل الملف
   os.remove(EXCEL_PATH)    # حذف الملف
   ```

3. **تحقق من المسار قبل الكتابة:**
   ```python
   def safe_write(path):
       forbidden = ["JEDCO_Fail_HT", "JEDCO_Tabel_Record", "input"]
       for f in forbidden:
           if f in str(path):
               raise ValueError(f"ممنوع الكتابة في: {path}")
   ```

---

## 🔧 إعدادات التطوير

### المكتبات المطلوبة:
```
pandas>=2.0.0
openpyxl>=3.1.0
```

### تشغيل السكربتات:
```bash
cd /c/Users/pcpz1/Desktop/jedco-dashboard-HT
python scripts/data_reader.py
```

---

## 📋 قائمة التحقق قبل كل عملية

- [ ] هل أقرأ من الملف الأصلي فقط؟
- [ ] هل أكتب في output/ فقط؟
- [ ] هل استخدمت read_only mode؟
- [ ] هل تجنبت to_excel على الملف الأصلي؟
- [ ] هل النتيجة JSON/CSV وليس Excel؟

---

## 🆘 في حالة الخطأ

إذا حدث خطأ أو تعديل غير مقصود:

1. **لا تُكمل** - توقف فوراً
2. **أخبر المستخدم** بالضبط ما حدث
3. **تحقق من الملف الأصلي** - هل تغير؟
4. **استخدم النسخة الاحتياطية** من مجلد الأرشيف

---

> آخر تحديث: 2026-01-24
